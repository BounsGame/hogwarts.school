SELECT * FROM student
WHERE age BETWEEN 10 AND 20;

SELECT name FROM student;

SELECT * FROM student
WHERE name LIKE '%o%';

SELECT * FROM student
WHERE age <= 25;

SELECT * FROM student
ORDER BY age;
